name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: prakhar2451
          password: ghp_NaF6iDOpdxuk50|13kbipC32M18NOn2lyZQI

      - name: Build Docker image
        run: |
          docker build . -t ghcr.io/${{ github.repository_owner }}/user-service:${{ github.sha }}

      - name: Push Docker image
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/user-service:${{ github.sha }}
